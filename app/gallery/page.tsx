import GalleryPage from "@/pages/Gallery/Gallery";
import React from "react";

export const metadata = {
  title: "Gallery - Lava Cafe",
  description:
    "Explore the vibrant atmosphere of Lava Cafe through our gallery. From cozy corners to lively events, see what makes every visit special.",
};

const Gallery = () => {
  return <GalleryPage />;
};

export default Gallery;
